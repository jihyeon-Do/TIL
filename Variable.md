## 변수

### 변수란 무엇인가? 왜 필요한가? 

- 변수는 데이터를 관리하기 위한 핵심 개념

- 애플리케이션은 데이터를 다룬다.  
- 보통 데이터를 **입력(input) ->  출력(output)**

```js
	10     +     20
// 좌변  연산자  우변 // 좌변가 우변의 숫자 값 = 피연산자
```

연산을 수행하기 위해서는 먼저 연산자의 좌변과 우변의 숫자 값 (= 피연산자) 를 기억해야 한다.



*컴퓨터는 메모리를 사용해 데이터를 기억한다*

**여기서 메모리란?**

=> 데이터를 저장할 수 있는 메모리 셀들의 집합체. 셀 하나의 크기는 1byte (8bit) 이며 컴퓨터는 셀의 크기, 즉 1byte 단위로 데이터를 저장(write)하거나 읽어(read)들인다. 컴퓨터는 **모든 데이터를 2진수로 처리한다.** 따라서 메모리에 저장되는 데이터는 종류와 상관없이 2진수다. 각각의 셀은 고유의 메모리주소를 갖는다. 메모리 주소는 공간의 위치를 나타내며 0부터 시작하여 메모리 크기만큼 정수로 표현된다. 

+++

위의 예제에서 

`10 + 20` 은 연산 결과가 필요하고 이를 사용해 무엇인가를 하겠다는 의도가 있었을텐데 연산결과인 30을 재사용할 수가 없다.  즉 연산 결과 30을 재사용하고 싶다면 메모리의 주소를 통해 결과 30이 저장된 메모리 공간에 직접 접근하여야 한다. 하지만 메모리에 직접접근하는 것은 치명적인 오류가 발생할 수도 있기 때문에 허용하지 않으며, 프로그래밍 언어를 기억하고 싶은 값을 메모리에 저장하고 저장된 값을 읽어 들여 재사용하기 위해서는 `변수`라는 메커니즘을 제공, 사용한다.



#### 변수 : 하나의 값을 저장하기 위해 확보한 메모리 공간 자체 또는 그 메모리 공간을 식별하기 위해 붙인 이름 (값의 위치를 가리키는 상징적인 이름)

```
변수에 여러개의 값을 저장하는 방법

변수는 원래 하나의 값을 저장하기 위한 메커니즘
단, 배열이나 객체와 같은 자료 구조를 사용하면 하나의 값처럼 사용할 수 있다.
```

```js
var userId = 1;
var uwerName ='Lee';
// 하나의 변수에 하나의 값을 저장

var user = { id : 1, name : 'Lee' };

var users = [
    {id : 1, name : 'Lee'},
    {id : 2, name : 'Kim'}
];
```

```js
var result = 10 + 20; // 30 => 변수 값
```

1. `10 + 20`은 연산을 통해 새로운값 30생성
2. 연산을 통해 생성된 값 30은 메모리 공간에 저장됨
3. 메모리 공간에 저장된 값 30을 다시 읽어들여서 재사용할 수 있도록 변수에 할당



- 메모리 공간에 저장된 값을 식별할 수 있는 고유한 이름 = 변수
- 변수 값을 저장하는 것을 할당(대입, 저장)이라 한다
- 변수에 저장된 값을 읽어 들이는 것을 **참조(reference)**라고 한다.

- 변수 이름을 통해 참조를 요청하면 
  변수 이름과 매핑된 메모리 주소를 통해 공간에 접근하여 값을 반환
- 의미를 가지는 변수이름은 가독성을 높여준다.



### 식별자

#### 식별자 : 어떤값을 구별하여 식별해낼 수 있는 고유한 이름

변수이름 = 식별자



값은 메모리 공간에 저장되어 있고, 식별자는 메모리 공간에 저장되어 있는 값을 구별하여 식별해 낼 수 있어야 한다.

이를 위해 식별자는 메모리 주소(공간)을 기억해야 한다.



- 식별자는 값이 저장되어있는 메모리 주소와 매핑관계를 가지며 이 매핑 정보도 메모리에 저장되어야 한다.
- 식별자는 변수, 함수, 클래스 등의 이름을 모두 포함한다.
- 메모리 상에 존재하는 어떤 값을 식별할 수 있는 이름은 모두 식별자라고 부른다.
- 식별자는 네이밍 규칙 준수하며, 선언에 의해 식별자의 존재를 알린다.



### 변수선언

변수 선언 = 변수를 생성

#### 변수를 사용하려면 반드시 선언이 필요하다. 변수를 선언할 때는 var, let, const 키워드를 사용한다.

```
var 키워드는 여러 단점이 있다.
* 블록 레벨 스코프 지원 x / 함수 레벨 스코프를 지원

보완하기 위해 let과 const 등장

ES6는 기본적으로 하위 호환성을 유지하면서 ES5의 기반위에 새로운 기능을 추가. 
** ES6 > ES5 **
```

```js
var score; // undefined : 자바스크립트가 제공하는 원시타입의 값
// 변수 선언 (선언문)
```

var 키워드는 뒤에 오는 변수 이름으로 새로운 변수를 선언할 것을 지시하는 키워드이다.

```
키워드 : 자바스크립트 코드를 해석하고 실행하는 자바스크립트 엔진이 수행할 동작을 규정한 일종의 명령어

var, let, const 가 있다.
```



- **자바스크립트 변수 선언 단계**
  - 선언 단계 : 변수 이름을 등록하여 자바스크립트 엔진에 변수의 존재를 알린다.
  - 초기화 단계 : 값을 저장하기 위한 메모리 공간을 확보하고 암묵적으로 `undefined` 를 할당한다.



- var 키워드를 사용한 변수 선언은 선언 단계와 초기화 단계가 동시에 진행된다. // 처음안 사실..!

- 선언단계를 통해 변수 이름을 등록하고, 초기화 단계를 통해 암묵적으로 `undefined`를 할당하여 초기화한다.
  - 초기화란? 변수가 선언된 이후 최초로 값을 할당하는 것
  - 초기화를 하지 않으면 확보된 메모리 공간에는 이전에 사용했던 값이 남아 있을 수 있다.
- 변수를 사용하려면 반드시 선언이 필요하다.
- 만약 선언하지 않은 식별자에 접근하면 참조에러(ReferenceError)가 발생한다.



### 변수 선언의 실행 시점과 변수 호이스팅

```js
console.log(score); // undefined

var scroe; // 변수 선언문

// 원래는 참조 에러가 발생하는게 맞지만 
// undefined가 출력된다.
// 이유는 변수 선언이 소스코드가 한줄씩 순차적으로 실행되는 시점, 즉 런타임이 아닌 그 이전단계에서 먼저 실행    되기 때문이다.
```

- 자바스크립트 코드는 인터프리터 - 한 줄씩 순차적으로 실행
- 코드의 평가과정( 모든 선언문을 찾아내어 먼저 실행 ) -> 런타임

- 변수 선언이 소스 코드 어디에 있던지 상관없이 다른 코드보다 먼저 실행한다,
  따라서 변수 선언이 소스코드 어디에 있던지 상관없이 변수를 참조할 수 있다.

#### 변수 호이스팅 : 변수 선언문이 코드의 선두로 끌어 올려진 것처럼 동작하는 자바스크립트 고유의 특징

- 변수 선언 뿐 아니라 키워드를 사용하여 선언된 모든 식별자 (변수, 함수, 클래스)는 호이스팅 된다.



### 값의 할당

변수에 값을 할당(대입,저장) 할때는 할당 연산자(=)를 이용한다.

할당 연산자는 우변의값 => 좌변의변수에 할당

```js
var score; // 변수선언
score = 80; // 값의 할당

// 단축문
var score = 80;
// 변수 선언과 값의 할당
```

**변수 선언은 소스코드가 순차적으로 실행되기 이전, 즉 런타임 이전에 먼저 실행되지만 값의 할당은 소스코드가 순차적으로 실행되는 시점인 런타임에 실행된다.**

````js
console.log(score); // undefined

var score = 80; // 변수 선언과 값의 할당

console.log(score); // 80

// 변수에 값을 할당하는 시점에는 이미 변수 선언이 완료된 상태이며 변수는 undefined로 초기화 되어 있다.
// 거기에 score 값을 할당하면 값은 undefined => 80 으로 새롭게 할당 받는다.
````

- 이는 변수 선언과 값의 할당을 각각 실행했을 때와 단축문으로 표현했을때 모두 동일하다.

```js
console.log(score); // undefined

var score; // 변수 선언
score = 80; // 값의 할당

console.log(score); // 80
```

- 변수의 선언과 값의 할당을 단축문으로 표현해도 변수 선언과 값의 할당은 분리해서 각각 실행한다.
- 변수를 초기화하고 값을 할당할 때 공간을 지우고 그 메모리를 공간에 값을 새롭게 할당하는것 x
- 새로운 메모리 공간을 확보하고 그 공간에 새로운 값을 저장한다.

```js
console.log(score); // undefined

score = 80; // 값의 할당 먼저
var score; // 그다음 변수 선언

// 결과는 어떻게 나올까?

console.log(score); // ??
```



### 값의 재할당

이번에는 score에 새로운 값을 재할당

재할당 : 이미 값이 할당되어 있는 변수에 새로운 값을 또다시 할당한다.

```js
var score = 80; // 단축문으로 변수선언, 값 할당

score = 90; // score에 90 새로운값 재할당

// 마찬가지로 80이 저장되이 있던 메모리 공간을 지우고 그 메모리공간에 재할당 하는 것이 아닌, 새로운 메모리 공간을 확보하고 그 메모리 공간에 값90을 재할당한다.
```

- var 키워드로 선언한 변수는 값을 재할당 할 수 있다.
- 재할당은 현재 변수가 저장하고 있는 값을 버리고 새로운값을 저장하는 것
- var 키워드로 선언한 변수는 선언과 동시에 undefined로 초기화



*변수는 항상 값이 변할수 있기 때문에 변수!!!!!*

**만약 재할당을 할 수 없어서 변수에 저장된 값을 변경할 수 없다면 변수가 아니라 상수라 부른다.*



```
const 키워드
- ES6에 도입
- 재할당 금지
- 상수로 표현된다 ( 반드시 상수만을 위해 사용하지는 x )
```



### 값의 교환

두 변수의 값을 교환하는 코드

```js
var x = 1;
var y = 2;

// do something

x = 2;
y = 1;

///

console.log(x,y); // 2 1
```



### 식별자 네이밍 규칙

#### 식별자 : 어떤값을 구별하여 식별해낼 수 있는 이름

식별자 네이밍 규칙

- 식별자는 특수문자를 제외한 문자, 숫자, underscore( _ ), 달러기호($)를 포함할 수 있다.

- 식별자는 특수문자를 제외한 문자, underscore( _ ), 달러 기호로 시작해야한다.
- 숫자로 시작 xxxxx
- 예약어는 식별자로 사용할 수 없다. (키워드도 포함)

```
예약어 : 프로그래밍 언어에서 사용되고 있는 언어들
```

변수도 식별자이기 때문에 네이밍 규칙 따라야 한다.

```js
var person, $elem, _name, first_name, val1;  // o

var first-name // - 기호 사용 x 
var 1st; // 숫자로 시작 x 
var this;  // this는 예약어 
// x 
```

ES5부터 식별자를 만들 때 유니코드 문자도 허용 ( 하지만 권장 x )

변수 이름은 의미가 명확해야한다. (가독성 높임)

```js
var x = 3; // 변수 x 가 의미하는 바를 알 수 없다.
var score = 100; // ok 변수 score는 점수를 의미한다.
```



**네이밍 컨벤션**

카멜케이스 

```js
var fitstName;
```

스네이크 케이스

```js
var first_name;
```

퍼스칼 케이스

```js
var FirstName;
```

헝가리언 케이스

```js
var strFirstName;
var $elem = $('.myClass');
```

가장 일반적으로 변수나 함수 이름에는 카멜케이스 사용

생성자 함수, 클래스 이름에는 파스칼케이스를 사용

